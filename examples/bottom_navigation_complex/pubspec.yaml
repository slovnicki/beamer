name: bottom_navigation_complex
description: Complex bottom navigation example app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  beamer:
    path: ../../package

  cupertino_icons: ^1.0.3
  lazy_load_indexed_stack: ^0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true